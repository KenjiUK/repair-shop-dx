# エンジンオイル交換 画面設計書

## ドキュメント情報

- **作成日**: 2025-01-XX
- **バージョン**: 1.0
- **対象**: エンジンオイル交換の各画面の詳細設計
- **設計方針**: 世界最高のUI/UX、ITコンサルの目線での推奨を元に設計
- **業務フローパターン**: パターンB（事前見積→了承→入庫→作業）

---

## 1. 画面一覧

### 1-1. 整備士向け画面

1. **診断画面** (`/mechanic/diagnosis/[id]`)
   - 簡易検査項目の入力（3項目）
   - 写真撮影（必要に応じて）
   - 部品リストアップ（イレギュラー用、通常は非表示）

2. **作業画面** (`/mechanic/work/[id]`)
   - 作業記録の入力
   - Before/After写真の撮影
   - 交換部品の記録

### 1-2. 特徴

- **見積もり画面・承認画面は不要**: 予約時に実施が確定しているため
- **簡易検査**: 3項目のみ（オイルの状態確認、オイルフィルターの確認、エアフィルターの確認）
- **所要時間**: 1時間以内で完了
- **写真撮影**: Before（抜いたオイル）、After（新しいオイル）

---

## 2. 診断画面の詳細設計

### 2-1. 画面構成

#### レイアウト構造

```
┌─────────────────────────────────────────┐
│ [←] 田中様 BMW X3 エンジンオイル交換診断 │
│ 整備士: 中村                             │
├─────────────────────────────────────────┤
│ 📋 車両情報（自動入力済み・編集不可）     │
│ ┌─────────────────────────────────────┐ │
│ │ 登録番号: 堺 330 す 1669            │ │
│ │ 車台番号: VF3CUHNZTHY061316         │ │
│ │ 初度登録: H29.10                    │ │
│ └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│ 🔍 簡易検査項目                          │
│ 進捗: ████████░░ 2/3項目完了 (67%)    │
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ 1. オイルの状態確認                  │ │
│ │ ─────────────────────────────────── │ │
│ │ オイルの色・汚れ具合:                │ │
│ │ [正常] [注意] [要交換]              │ │
│ │                                      │ │
│ │ オイル量（レベル点検）:              │ │
│ │ [正常] [不足] [過多]                │ │
│ │                                      │ │
│ │ [📷 写真を追加] [💬 コメント]       │ │
│ └─────────────────────────────────────┘ │
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ 2. オイルフィルターの確認            │ │
│ │ ─────────────────────────────────── │ │
│ │ フィルターの状態:                    │ │
│ │ [正常] [注意] [要交換]              │ │
│ │                                      │ │
│ │ 交換の有無:                          │ │
│ │ [交換済み] [交換不要] [要交換]      │ │
│ │ （予約時に確認済み）                 │ │
│ │                                      │ │
│ │ [📷 写真を追加] [💬 コメント]       │ │
│ └─────────────────────────────────────┘ │
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ 3. エアフィルターの確認               │ │
│ │ ─────────────────────────────────── │ │
│ │ フィルターの汚れ具合:                │ │
│ │ [正常] [注意] [要交換]              │ │
│ │                                      │ │
│ │ 交換の必要性:                        │ │
│ │ [交換不要] [推奨] [要交換]          │ │
│ │                                      │ │
│ │ [📷 写真を追加] [💬 コメント]       │ │
│ └─────────────────────────────────────┘ │
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ 🔧 追加部品リスト（イレギュラー用）   │ │
│ │ [＋部品を追加]                       │ │
│ │                                      │ │
│ │ （通常は非表示、追加部品が見つかった │ │
│ │   場合のみ表示）                     │ │
│ └─────────────────────────────────────┘ │
│                                          │
│ [一時保存] [診断完了]                    │
└─────────────────────────────────────────┘
```

### 2-2. コンポーネント設計

#### 2-2-1. 車両情報カード

**目的**: 診断対象の車両情報を表示（誤操作防止のため編集不可）

**表示項目**:
- 依頼者(使用者)の氏名又は名称
- 車名及び型式
- 自動車登録番号又は車両番号
- 原動機の型式
- 初度登録年又は初度検査年
- 車台番号

**データソース**: Google Sheets「【DB】車両マスタ」から自動取得

**UI/UXのポイント**:
- 背景色を薄くして、編集不可であることを視覚的に示す
- アイコン（🔒）で編集不可であることを示す

#### 2-2-2. 簡易検査項目カード

**目的**: 3つの簡易検査項目の状態を入力

**検査項目**:
1. **オイルの状態確認**
   - オイルの色・汚れ具合: 正常/注意/要交換
   - オイル量（レベル点検）: 正常/不足/過多
2. **オイルフィルターの確認**
   - フィルターの状態: 正常/注意/要交換
   - 交換の有無: 交換済み/交換不要/要交換（予約時に確認済み）
3. **エアフィルターの確認**
   - フィルターの汚れ具合: 正常/注意/要交換
   - 交換の必要性: 交換不要/推奨/要交換

**状態選択ボタンのデザイン**:
- **正常**: 緑色、チェックマークアイコン
- **注意**: 黄色、警告アイコン
- **要交換**: 赤色、Xアイコン
- **不足/過多**: 黄色、警告アイコン（オイル量のみ）
- **交換済み/交換不要**: 緑色、チェックマークアイコン
- **推奨**: 青色、情報アイコン

**UI/UXのポイント**:
- ボタンは画面幅いっぱいに配置（タッチしやすい）
- 選択された状態は明確に表示（背景色、アイコン）
- 写真ボタンは撮影済みの場合、サムネイルを表示
- コメントがある場合、アイコンで表示
- 各項目は独立したカードとして表示（視認性向上）

#### 2-2-3. 進捗バー

**目的**: 簡易検査の進捗を可視化

**表示内容**:
- 完了項目数 / 全項目数（3項目）
- 進捗パーセンテージ
- プログレスバー（視覚的）

**UI/UXのポイント**:
- 色で進捗を表現（0-50%: 赤、50-80%: 黄、80-100%: 緑）
- 数値と視覚的表現の両方で進捗を表示

#### 2-2-4. 追加部品リストセクション

**目的**: イレギュラーで追加部品が見つかった場合の記録

**表示条件**:
- 通常は非表示
- 「＋部品を追加」ボタンをタップした場合のみ表示

**機能**:
- 部品名の入力
- 緊急度の選択（緊急/推奨/任意）
- 推奨理由の入力
- 写真の追加
- 削除機能

**UI/UXのポイント**:
- 基本は非表示（エンジンオイル交換では通常不要）
- 必要になった場合のみ表示（イレギュラー対応）

#### 2-2-5. 一時保存ボタン

**目的**: 現在の入力内容を保存（自動保存も実施）

**UI/UXのポイント**:
- 保存成功時にトースト通知（邪魔にならない程度に）
- オフライン時も保存可能（ローカルストレージ）
- 自動保存も実施（30秒ごと、または項目変更時）

#### 2-2-6. 診断完了ボタン

**目的**: 診断を完了し、作業画面への遷移を可能にする

**UI/UXのポイント**:
- 全ての項目が完了していない場合、警告を表示（任意）
- 確認ダイアログを表示（任意、簡易検査のため）
- 完了後、作業画面への遷移を可能にする

### 2-3. データフロー

```
1. 画面読み込み
   ↓
2. 車両情報を取得（Google Sheets）
   ↓
3. 既存の診断結果を取得（Zoho CRM）
   ↓
4. 簡易検査項目を表示
   ↓
5. 整備士が各項目の状態を選択
   ↓
6. 自動保存（30秒ごと、または項目変更時）
   ↓
7. 診断完了ボタンをタップ
   ↓
8. 診断結果を保存（Zoho CRM）
   ↓
9. 作業画面への遷移を可能にする
```

### 2-4. インタラクション設計

#### 2-4-1. 状態選択

- **タップ**: 状態を選択
- **選択済み**: 背景色とアイコンで明確に表示
- **変更**: 別の状態をタップすると、前の選択が解除され、新しい状態が選択される

#### 2-4-2. 写真撮影

- **タップ**: カメラを起動
- **撮影**: 写真を撮影
- **確認**: 撮影後、確認画面を表示（再撮影可能）
- **保存**: 確認後、写真を保存（Google Drive）
- **表示**: 撮影済みの場合、サムネイルを表示

#### 2-4-3. コメント入力

- **タップ**: コメント入力ダイアログを表示
- **入力**: テキストを入力
- **保存**: 保存ボタンをタップして保存
- **表示**: コメントがある場合、アイコンで表示

#### 2-4-4. エラーハンドリング

- **ネットワークエラー**: オフライン時はローカルストレージに保存、オンライン時に自動同期
- **保存エラー**: エラーメッセージを表示、再試行ボタンを表示
- **写真アップロードエラー**: エラーメッセージを表示、再アップロードボタンを表示

### 2-5. 状態管理

- **ローディング**: 車両情報取得中、診断結果保存中
- **エラー**: ネットワークエラー、保存エラー
- **成功**: 診断完了、作業画面への遷移可能

---

## 3. 作業画面の詳細設計

### 3-1. 画面構成

#### レイアウト構造

```
┌─────────────────────────────────────────┐
│ [←] 田中様 BMW X3 エンジンオイル交換作業 │
│ 整備士: 中村                             │
├─────────────────────────────────────────┤
│ 📋 車両情報（自動入力済み・編集不可）     │
│ ┌─────────────────────────────────────┐ │
│ │ 登録番号: 堺 330 す 1669            │ │
│ │ 車台番号: VF3CUHNZTHY061316         │ │
│ │ 初度登録: H29.10                    │ │
│ └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│ 📝 作業記録                              │
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ 交換したオイル                       │ │
│ │ ─────────────────────────────────── │ │
│ │ 種類: [SM規格全合成油]              │ │
│ │ 量:   [3] L                         │ │
│ │                                      │ │
│ │ 交換部品:                            │ │
│ │ [✓] オイルフィルター                 │ │
│ │                                      │ │
│ │ 追加部品（イレギュラー）:            │ │
│ │ [＋部品を追加]                       │ │
│ └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│ 📸 Before/After写真                     │
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ Before: 抜いたオイルの状態           │ │
│ │ [📷 写真を追加]                      │ │
│ │                                      │ │
│ │ [写真1] [写真2] [写真3]              │ │
│ │ （サムネイル表示、タップで拡大）     │ │
│ └─────────────────────────────────────┘ │
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ After: 新しいオイルを入れた状態      │ │
│ │ [📷 写真を追加]                      │ │
│ │                                      │ │
│ │ [写真1] [写真2]                      │ │
│ │ （サムネイル表示、タップで拡大）     │ │
│ └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│ 📋 簡易検査結果（診断画面から引き継ぎ）  │
│ ┌─────────────────────────────────────┐ │
│ │ 1. オイルの状態確認: [正常]          │ │
│ │ 2. オイルフィルターの確認: [正常]    │ │
│ │ 3. エアフィルターの確認: [注意]      │ │
│ └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│ 💬 作業メモ                             │
│ ┌─────────────────────────────────────┐ │
│ │                                     │ │
│ │  自由記述欄                          │ │
│ │  （複数行入力可能）                  │ │
│ │                                     │ │
│ └─────────────────────────────────────┘ │
│                                          │
│ [一時保存] [作業完了]                    │
└─────────────────────────────────────────┘
```

### 3-2. コンポーネント設計

#### 3-2-1. 車両情報カード

**目的**: 作業対象の車両情報を表示（誤操作防止のため編集不可）

**表示項目**: 診断画面と同様

**データソース**: Google Sheets「【DB】車両マスタ」から自動取得

#### 3-2-2. 作業記録セクション

**目的**: 交換したオイルと部品を記録

**入力項目**:
- **交換したオイル**
  - 種類: テキスト入力（デフォルト: SM規格全合成油）
  - 量: 数値入力（デフォルト: 3L）
- **交換部品**
  - オイルフィルター: チェックボックス（デフォルト: チェック済み）
- **追加部品（イレギュラー）**
  - 部品名、数量の入力（通常は不要）

**UI/UXのポイント**:
- オイルの種類はドロップダウンまたはテキスト入力
- 量は数字キーパッドを表示
- 交換部品はチェックボックスで選択
- 追加部品は「＋部品を追加」ボタンで追加

#### 3-2-3. Before/After写真セクション

**目的**: 作業前後の写真を撮影・表示

**Before写真**:
- **目的**: 抜いたオイルの状態を記録（どれだけ黒くなったか）
- **撮影タイミング**: オイルを抜いた後、新しいオイルを入れる前
- **撮影枚数**: 1-3枚推奨

**After写真**:
- **目的**: 新しいオイルを入れた状態を記録
- **撮影タイミング**: 新しいオイルを入れた後
- **撮影枚数**: 1-2枚推奨

**UI/UXのポイント**:
- 写真ボタンは大きく表示（タッチしやすい）
- 撮影済みの場合、サムネイルを表示
- サムネイルをタップすると拡大表示
- 複数枚撮影可能
- 写真の削除機能

#### 3-2-4. 簡易検査結果表示

**目的**: 診断画面で入力した簡易検査結果を表示（参考用）

**表示内容**:
- 各検査項目の状態（正常/注意/要交換）
- 診断画面から自動で引き継ぎ

**UI/UXのポイント**:
- 読み取り専用（編集不可）
- 背景色を薄くして、参考情報であることを示す

#### 3-2-5. 作業メモ入力欄

**目的**: 作業に関する自由記述

**機能**:
- 複数行入力可能
- 文字数制限なし（ただし、システムの制限内）

**UI/UXのポイント**:
- テキストエリアで入力
- プレースホルダーで入力例を表示

#### 3-2-6. 一時保存ボタン

**目的**: 現在の入力内容を保存（自動保存も実施）

**UI/UXのポイント**:
- 保存成功時にトースト通知
- オフライン時も保存可能（ローカルストレージ）
- 自動保存も実施（30秒ごと、または項目変更時）

#### 3-2-7. 作業完了ボタン

**目的**: 作業を完了し、フロントスタッフに通知

**UI/UXのポイント**:
- Before/After写真が撮影されていない場合、警告を表示（推奨）
- 確認ダイアログを表示
- 完了後、フロントスタッフに通知
- ステータスを「作業完了」に更新

### 3-3. データフロー

```
1. 画面読み込み
   ↓
2. 車両情報を取得（Google Sheets）
   ↓
3. 診断結果を取得（Zoho CRM）
   ↓
4. 作業記録を表示（既存の記録がある場合）
   ↓
5. 整備士が作業記録を入力
   ↓
6. Before/After写真を撮影
   ↓
7. 自動保存（30秒ごと、または項目変更時）
   ↓
8. 作業完了ボタンをタップ
   ↓
9. 作業記録を保存（Zoho CRM）
   ↓
10. 写真をアップロード（Google Drive）
   ↓
11. フロントスタッフに通知
   ↓
12. ステータスを「作業完了」に更新
```

### 3-4. インタラクション設計

#### 3-4-1. 作業記録入力

- **オイルの種類**: テキスト入力またはドロップダウン選択
- **オイルの量**: 数字キーパッドを表示
- **交換部品**: チェックボックスで選択
- **追加部品**: 「＋部品を追加」ボタンで追加

#### 3-4-2. 写真撮影

- **タップ**: カメラを起動
- **撮影**: 写真を撮影
- **確認**: 撮影後、確認画面を表示（再撮影可能）
- **保存**: 確認後、写真を保存（Google Drive）
- **表示**: 撮影済みの場合、サムネイルを表示
- **拡大**: サムネイルをタップすると拡大表示
- **削除**: 写真を削除（確認ダイアログ表示）

#### 3-4-3. エラーハンドリング

- **ネットワークエラー**: オフライン時はローカルストレージに保存、オンライン時に自動同期
- **保存エラー**: エラーメッセージを表示、再試行ボタンを表示
- **写真アップロードエラー**: エラーメッセージを表示、再アップロードボタンを表示

### 3-5. 状態管理

- **ローディング**: 車両情報取得中、作業記録保存中、写真アップロード中
- **エラー**: ネットワークエラー、保存エラー、写真アップロードエラー
- **成功**: 作業完了、フロントスタッフへの通知完了

---

## 4. データモデル

### 4-1. エンジンオイル交換ジョブデータ

```typescript
/**
 * エンジンオイル交換ジョブ
 */
export interface EngineOilChangeJob {
  /** Job ID */
  jobId: string;
  /** 入庫区分 */
  serviceKind: "エンジンオイル交換";
  
  /** 車両情報 */
  vehicle: {
    /** 車両ID */
    vehicleId: string;
    /** 走行距離 */
    mileage: number;
  };
  
  /** 診断情報 */
  diagnosis: {
    /** 簡易検査結果 */
    inspectionResults: InspectionResult[];
    /** 診断写真 */
    photos: Photo[];
    /** 追加部品リスト（イレギュラー用） */
    additionalParts?: PartItem[];
    /** コメント（整備士の所見） */
    comments: string;
  };
  
  /** 作業情報 */
  work: {
    /** 交換したオイル */
    oil: {
      /** 種類 */
      type: string;
      /** 量（L） */
      quantity: number;
    };
    /** 交換部品 */
    replacedParts: string[];
    /** 追加部品（イレギュラー用） */
    additionalParts?: PartItem[];
    /** Before写真 */
    beforePhotos: Photo[];
    /** After写真 */
    afterPhotos: Photo[];
    /** 作業メモ */
    notes: string;
    /** 作業完了日時 */
    completedAt?: Date;
  };
  
  /** ステータス */
  status: "入庫待ち" | "診断中" | "作業待ち" | "作業中" | "作業完了" | "出庫待ち";
}

/**
 * 簡易検査結果
 */
export interface InspectionResult {
  /** 項目名 */
  itemName: string;
  /** 状態 */
  condition: "正常" | "注意" | "要交換";
  /** 詳細状態（オイル量など） */
  detailCondition?: "正常" | "不足" | "過多" | "交換済み" | "交換不要" | "要交換" | "推奨";
  /** 写真 */
  photo?: Photo;
  /** コメント */
  comment?: string;
}

/**
 * 追加部品項目（イレギュラー用）
 */
export interface PartItem {
  /** 部品名 */
  name: string;
  /** 数量 */
  quantity: number;
  /** 単位 */
  unit: string;
  /** 緊急度 */
  urgency?: "緊急" | "推奨" | "任意";
  /** 推奨理由 */
  reason?: string;
  /** 写真 */
  photo?: Photo;
}
```

---

## 5. API設計

### 5-1. 診断結果の保存API

**エンドポイント**: `POST /api/jobs/[id]/diagnosis`

**リクエストボディ**:
```typescript
{
  inspectionResults: InspectionResult[];
  photos: Photo[];
  additionalParts?: PartItem[];
  comments: string;
}
```

**レスポンス**:
```typescript
{
  success: boolean;
  jobId: string;
  status: string;
}
```

### 5-2. 作業記録の保存API

**エンドポイント**: `POST /api/jobs/[id]/work`

**リクエストボディ**:
```typescript
{
  oil: {
    type: string;
    quantity: number;
  };
  replacedParts: string[];
  additionalParts?: PartItem[];
  beforePhotos: Photo[];
  afterPhotos: Photo[];
  notes: string;
}
```

**レスポンス**:
```typescript
{
  success: boolean;
  jobId: string;
  status: string;
}
```

### 5-3. 写真アップロードAPI

**エンドポイント**: `POST /api/photos/upload`

**リクエスト**: FormData（multipart/form-data）

**レスポンス**:
```typescript
{
  success: boolean;
  photoId: string;
  photoUrl: string;
  googleDriveFileId: string;
}
```

---

## 6. ユーザージャーニー

### 6-1. パターンBのフロー（事前見積→了承→入庫→作業）

```
【予約時】
1. 顧客がエンジンオイル交換を予約
2. フロントスタッフがメール等で見積もりを送信（¥10,560）
3. 顧客が了承
4. 入庫日を確定

【入庫当日】
5. フロントスタッフが受付
   - 走行距離を入力
   - 車両情報を確認
   - ステータスを「診断中」に更新

6. 整備士が診断画面を開く
   - 車両情報を確認（自動入力済み）
   - 簡易検査を実施
     - オイルの状態確認
     - オイルフィルターの確認
     - エアフィルターの確認
   - 必要に応じて写真を撮影
   - イレギュラーで追加部品が見つかった場合、部品リストに追加
   - 「診断完了」ボタンをタップ
   - ステータスを「作業待ち」に更新

7. 整備士が作業画面を開く
   - 診断結果を確認（参考用）
   - 作業を実施
   - 作業記録を入力
     - 交換したオイルの種類・量
     - 交換部品（オイルフィルター）
   - Before写真を撮影（抜いたオイル）
   - After写真を撮影（新しいオイル）
   - 作業メモを入力（必要に応じて）
   - 「作業完了」ボタンをタップ
   - ステータスを「作業完了」に更新
   - フロントスタッフに通知

8. フロントスタッフが引渡し・説明
   - 作業内容を確認
   - 顧客に説明
   - ステータスを「出庫待ち」に更新

9. 出庫完了
   - ステータスを「完了」に更新
```

### 6-2. 各画面でのユーザー操作の詳細

#### 6-2-1. 診断画面での操作

1. **画面を開く**
   - 車両情報が自動で表示される
   - 既存の診断結果がある場合、自動で読み込まれる

2. **簡易検査を実施**
   - 各項目の状態を選択
   - 必要に応じて写真を撮影
   - コメントを入力

3. **追加部品の記録（イレギュラーの場合）**
   - 「＋部品を追加」ボタンをタップ
   - 部品名、数量、緊急度、推奨理由を入力
   - 写真を追加（必要に応じて）

4. **診断完了**
   - 「診断完了」ボタンをタップ
   - 確認ダイアログを表示（任意）
   - 診断結果を保存
   - 作業画面への遷移を可能にする

#### 6-2-2. 作業画面での操作

1. **画面を開く**
   - 車両情報が自動で表示される
   - 診断結果が自動で表示される（参考用）
   - 既存の作業記録がある場合、自動で読み込まれる

2. **作業記録を入力**
   - 交換したオイルの種類・量を入力
   - 交換部品を選択（オイルフィルター）
   - 追加部品を記録（イレギュラーの場合）

3. **Before/After写真を撮影**
   - Before写真を撮影（抜いたオイル）
   - After写真を撮影（新しいオイル）
   - 写真を確認・削除（必要に応じて）

4. **作業メモを入力**
   - 作業に関する自由記述を入力

5. **作業完了**
   - 「作業完了」ボタンをタップ
   - Before/After写真が撮影されていない場合、警告を表示（推奨）
   - 確認ダイアログを表示
   - 作業記録を保存
   - 写真をアップロード
   - フロントスタッフに通知

---

## 7. 実装の優先順位

### Phase 1: MVP（最小限の機能）

1. **診断画面の基本機能**
   - 簡易検査項目の表示・入力
   - 状態の選択（正常/注意/要交換）
   - データの保存

2. **作業画面の基本機能**
   - 作業記録の入力
   - Before/After写真の撮影
   - データの保存

### Phase 2: 拡張機能

1. **写真機能の強化**
   - 写真の拡大表示
   - 写真の削除機能

2. **追加部品機能**
   - イレギュラー時の追加部品記録

3. **自動保存機能**
   - 30秒ごとの自動保存
   - オフライン対応

### Phase 3: 高度な機能

1. **通知機能**
   - フロントスタッフへの通知

2. **レポート機能**
   - 顧客向けレポート画面での表示

---

## 更新履歴

- 2025-01-XX: 初版作成（詳細画面設計書）

























