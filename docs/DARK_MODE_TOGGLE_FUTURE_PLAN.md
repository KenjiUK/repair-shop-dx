# ダークモード切り替え機能の実装計画

## 概要
現在、ダークモード切り替えボタンはヘッダーから削除されています。将来的に実装する際の計画を記録します。

## 削除日
2025-01-XX（実際の日付を記録）

## 削除理由
- 現在の実装では不要と判断
- 後で実装する予定のため、一時的に削除

## 現在の状態
- `ThemeToggleButton`コンポーネントは`src/components/features/theme-toggle-button.tsx`に存在
- ヘッダー（`src/components/layout/app-header.tsx`）からは削除済み

## 将来の実装場所
以下の場所で使用されていました（実装時は再追加が必要）：
- トップページのヘッダー（通常表示時）
- トップページのヘッダー（スクロール時）
- その他ページのヘッダー（展開時）
- その他ページのヘッダー（縮小時）

## 実装時の考慮事項
1. モバイルでの表示位置を検討（右上が混雑しないように）
2. アクセシビリティの確保（キーボード操作、スクリーンリーダー対応）
3. ユーザーの設定をローカルストレージに保存


