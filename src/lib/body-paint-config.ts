/**
 * 板金・塗装系：設定
 *
 * 板金・塗装の損傷箇所と外注先管理の設定を定義
 */

// =============================================================================
// 型定義
// =============================================================================

/**
 * 損傷の種類
 */
export type DamageType = "板金" | "塗装" | "板金・塗装";

/**
 * 損傷の程度
 */
export type DamageSeverity = "軽微" | "中程度" | "深刻";

/**
 * 車体部位
 */
export type BodyPart =
  | "ボンネット"
  | "フロントバンパー"
  | "フロント左ドア"
  | "フロント右ドア"
  | "リア左ドア"
  | "リア右ドア"
  | "リアバンパー"
  | "トランク"
  | "ルーフ"
  | "左フェンダー"
  | "右フェンダー"
  | "左サイドミラー"
  | "右サイドミラー"
  | "その他";

/**
 * 外注先への発注方法
 */
export type OrderMethod = "写真送付" | "持ち込み";

/**
 * 外注先の作業進捗状況
 */
export type OutsourcingProgress = "発注済み" | "作業中" | "作業完了" | "引き取り済み";

/**
 * 事故の程度
 */
export type AccidentSeverity = "軽微" | "中程度" | "深刻" | "全損";

/**
 * 事故の種類
 */
export type AccidentType = "追突" | "出会い頭" | "側面衝突" | "単独事故" | "その他";

// =============================================================================
// 車体部位マスタ
// =============================================================================

/**
 * 車体部位一覧
 */
export const BODY_PARTS: BodyPart[] = [
  "ボンネット",
  "フロントバンパー",
  "フロント左ドア",
  "フロント右ドア",
  "リア左ドア",
  "リア右ドア",
  "リアバンパー",
  "トランク",
  "ルーフ",
  "左フェンダー",
  "右フェンダー",
  "左サイドミラー",
  "右サイドミラー",
  "その他",
];

/**
 * 損傷の種類一覧
 */
export const DAMAGE_TYPES: DamageType[] = ["板金", "塗装", "板金・塗装"];

/**
 * 損傷の程度一覧
 */
export const DAMAGE_SEVERITIES: DamageSeverity[] = ["軽微", "中程度", "深刻"];

/**
 * 外注先への発注方法一覧
 */
export const ORDER_METHODS: OrderMethod[] = ["写真送付", "持ち込み"];

/**
 * 外注先の作業進捗状況一覧
 */
export const OUTSOURCING_PROGRESSES: OutsourcingProgress[] = [
  "発注済み",
  "作業中",
  "作業完了",
  "引き取り済み",
];

/**
 * 事故の程度一覧
 */
export const ACCIDENT_SEVERITIES: AccidentSeverity[] = ["軽微", "中程度", "深刻", "全損"];

/**
 * 事故の種類一覧
 */
export const ACCIDENT_TYPES: AccidentType[] = [
  "追突",
  "出会い頭",
  "側面衝突",
  "単独事故",
  "その他",
];









